{
  "name": "@vayada/domain-intelligence",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Ask Intelligence domain contracts and agent runtime boundary for the TypeScript backend.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "prebuild": "npm --workspace @vayada/backend-auth run build",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "npm --workspace @vayada/backend-auth run build",
    "test": "vitest run",
    "pretypecheck": "npm --workspace @vayada/backend-auth run build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@openai/agents": "^0.11.6",
    "@vayada/backend-auth": "0.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^4.0.15"
  }
}

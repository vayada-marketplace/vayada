Implementation tickets (the B-track, in the audits' replacement order):
- VAY-763 — B1: Booking settings target read/write repository (includes removing the guest-form PMS sync; writes stay on legacy until the cutover write-freeze)
- VAY-764 — B2: Public hotel profile, host resolution, and custom-domain verification on target read models
- VAY-767 — B3: Quote/offers/calendar from target distribution read models (blocked by B2 — it needs the canonical profile projection)
- VAY-771 — B4: Booking Web checkout and guest lifecycle commands on target handlers with PMS reservation handoff — the highest-risk write surface, flagged as rehearsal-gating
- VAY-765 — Booking reservations read model (replaces the PMS-DB compatibility query)
- VAY-766 — Marketplace discovery on TARGET_DATABASE_URL (under the Marketplace milestone; VAY-758's fallback removal still waits on cutover)
- VAY-769 — Retire the booking telemetry forwarding bridge
- VAY-768 — Affiliate route ownership decision + implement or retire

Planning tickets (next contract-first audits):
- VAY-770 — PMS operations route contracts (P1/P2 — the 157-route pms-api surface, contracts before implementation)
- VAY-772 — Channex/webhook cutover plan (external providers retry and replay, so this can't be a config flip; it also owns the legacy scheduler shutdown matrix)

Every implementation ticket carries the audit's activation pattern: merge dark behind a *_SOURCE=target knob, with a startup assertion that the routes mount and pass contract tests with the legacy env vars unset — so cutover day is a sequence of config flips that were each already proven in staging.

What I'd start with: VAY-763 (B1) — it's the smallest, the contracts and tests already exist from the settings cutover, and it proves the dark-merge + activation-knob pattern everything else reuses. VAY-770 and VAY-772 are planning work that can run in parallel any time. Note the staging-rehearsal milestone stays empty until B1–B4, P1–P2, and F1 are far enough along, per the inventory's rehearsal gates — F1 (finance/payouts) and the marketplace V2–V7 verticals are the remaining unticketed implementation chunks.
what can we parallezie?

---

proceed

---

<subagent_notification>
{"agent_path":"019eb734-d993-7da0-8dd6-3edfd165bf43","status":{"completed":"**Findings**\n\n- **P1: Migration is untracked and would be omitted from the PR.**  \n  [packages/backend-migration/migrations/0012_booking_settings.sql](/Users/flamurmaliqi/git/vayada/packages/backend-migration/migrations/0012_booking_settings.sql:1) shows as `??` in `git status --short`. If the PR is created from committed/staged tracked changes, the target table will not ship while code queries `booking.booking_settings`. Add the migration file before committing.\n\n- **P1: Target-schema fixture AC is not satisfied.**  \n  [0012_booking_settings.sql](/Users/flamurmaliqi/git/vayada/packages/backend-migration/migrations/0012_booking_settings.sql:9) only creates an empty table, and [registry.ts](/Users/flamurmaliqi/git/vayada/packages/backend-migration/src/cases/registry.ts:22) has no booking-settings fixture/transform/parity case. The new route test uses an in-memory pool, so it does not prove existing settings contracts against target-schema fixtures. Add a target fixture/backfill path for `booking.booking_settings` and run the settings contracts against it.\n\n- **P2: Target repository ignores inactive source links.**  \n  Reads and writes join `hotel_catalog.property_source_links` without checking `status = 'active'` or `relationship = 'canonical_input'`: [bookingSettings.ts](/Users/flamurmaliqi/git/vayada/apps/api/src/routes/bookingSettings.ts:560), [bookingSettings.ts](/Users/flamurmaliqi/git/vayada/apps/api/src/routes/bookingSettings.ts:847). The schema explicitly supports `active`, `superseded`, and `ignored` links in [0004_property_catalog.sql](/Users/flamurmaliqi/git/vayada/packages/backend-migration/migrations/0004_property_catalog.sql:35). This can let a stale booking hotel ID read/write current target settings. Filter both paths to active canonical links and add a fixture/test for inactive links returning 404.\n\n- **P2: Strict target-mode legacy isolation is not enforced app-wide.**  \n  With `BOOKING_SETTINGS_SOURCE=target`, [server.ts](/Users/flamurmaliqi/git/vayada/apps/api/src/server.ts:53) still instantiates `createPgPublicHotelProfileRepository` whenever `BOOKING_DATABASE_URL` is present. If the AC means no target-mode code path touches `BOOKING_DATABASE_URL`, this violates it. Gate or clarify this scope, and add a server-level config test for target mode with legacy URLs present/unset."}}
</subagent_notification>